// Redirect to the new socket-server API route
export default function handler(req, res) {
    res.redirect(301, '/api/socket-server');
}

export const config = {
    api: {
        bodyParser: false,
    },
};
